spring:
  application:
    name: project_RBPO

  datasource:
    url: jdbc:h2:file:./data/socialnetwork
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    ru.mtuci.coursemanagement: DEBUG
    org.springframework.security: INFO
    org.springframework.web: INFO

jwt:
  secret: ${JWT_SECRET:0123456789012345678901234567890123456789012345678901234567890123}
  access-ttl-minutes: 15
  refresh-ttl-days: 7

server:
  port: 8443
  ssl:
    enabled: true
    key-store: file:C:/Users/Admin/Desktop/Matorin/project_RBPO/certs/out/rbpo-server-1БИК23522.jks
    key-store-type: PKCS12
    key-store-password: ServerPass_ChangeMe_123!
    key-alias: rbposrv1бик23522